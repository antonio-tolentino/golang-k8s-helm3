apiVersion: v1
kind: Pod
metadata:
  labels:
    sidecar: sonar-scanner-cli
  namespace: jenkins
spec:
  containers:
  - name: sonar-scanner-cli
    image: docker.io/sonarsource/sonar-scanner-cli:4.2
    #imagePullPolicy: Always
    command: 
    - cat
    tty: true
